# Code generated by @qninhdt/typespec-orm. DO NOT EDIT.
# Source: https://github.com/qninhdt/typespec-libraries

from typing import TYPE_CHECKING
from uuid import UUID
from enum import Enum

from sqlalchemy import Column, Enum as SAEnum, ForeignKey, Text
from sqlalchemy.dialects.postgresql import UUID as PG_UUID
from sqlmodel import Field, Relationship, SQLModel
from ..shared.entity import Entity


if TYPE_CHECKING:
    from .assistant_conversation import AssistantConversation


class MessageRole(str, Enum):
    """Auto-generated enum for message_role."""

    user = "user"
    assistant = "assistant"
    system = "system"


class AssistantMessage(Entity, table=True):
    """Persisted conversation message for product history and context rebuild."""

    __tablename__ = "assistant_messages" # type: ignore

    conversation_id: UUID = Field(sa_column=Column(PG_UUID(as_uuid=True), ForeignKey("assistant_conversations.id", ondelete="CASCADE"), nullable=False, index=True))
    role: MessageRole = Field(sa_column=Column(SAEnum(MessageRole), nullable=False))
    content: str = Field(sa_column=Column(Text, nullable=False))
    model_name: str | None = Field(default=None, max_length=120)
    token_count: int | None = Field(default=None)

    # ─── Relationships ─────────────────────
    conversation: AssistantConversation | None = Relationship()
