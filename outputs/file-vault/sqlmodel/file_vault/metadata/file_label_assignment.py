# Code generated by @qninhdt/typespec-orm. DO NOT EDIT.
# Source: https://github.com/qninhdt/typespec-libraries

from typing import TYPE_CHECKING
from uuid import UUID

from sqlalchemy import Column, ForeignKey, UniqueConstraint
from sqlalchemy.dialects.postgresql import UUID as PG_UUID
from sqlmodel import Field, Relationship, SQLModel
from ..shared.entity import Entity


if TYPE_CHECKING:
    from .file_label import FileLabel
    from .file_metadata import FileMetadata


class FileLabelAssignment(Entity, table=True):
    """Label assignment to a file."""

    __tablename__ = "file_label_assignments"  # type: ignore
    __table_args__ = (
        UniqueConstraint(
            "file_id", "label_id", name="file_label_assignments_file_id_label_id_unique"
        ),
    )

    file_id: UUID = Field(
        sa_column=Column(
            PG_UUID(as_uuid=True),
            ForeignKey("file_metadata.id", ondelete="CASCADE"),
            nullable=False,
            index=True,
        )
    )
    label_id: UUID = Field(
        sa_column=Column(
            PG_UUID(as_uuid=True),
            ForeignKey("file_labels.id", ondelete="CASCADE"),
            nullable=False,
            index=True,
        )
    )

    # ─── Relationships ─────────────────────
    file: FileMetadata | None = Relationship()
    label: FileLabel | None = Relationship()
