# Code generated by @qninhdt/typespec-orm. DO NOT EDIT.
# Source: https://github.com/qninhdt/typespec-libraries

from typing import TYPE_CHECKING
from uuid import UUID
from datetime import datetime
from enum import Enum

from sqlalchemy import Column, DateTime, Enum as SAEnum, ForeignKey
from sqlmodel import Field, Relationship, SQLModel
from ..shared.entity import Entity


if TYPE_CHECKING:
    from .notification import Notification


class NotificationChannel(str, Enum):
    """Auto-generated enum for notification_channel."""

    in_app = "in_app"
    email = "email"
    webhook = "webhook"


class NotificationState(str, Enum):
    """Auto-generated enum for notification_state."""

    queued = "queued"
    sent = "sent"
    failed = "failed"


class NotificationDelivery(Entity, table=True):
    """Delivery attempt through a concrete channel."""

    __tablename__ = "notification_deliveries"  # type: ignore

    notification_id: UUID = Field(
        sa_column=Column(
            ForeignKey("notifications.id", ondelete="CASCADE"),
            nullable=False,
            index=True,
        )
    )
    channel: NotificationChannel = Field(
        sa_column=Column(SAEnum(NotificationChannel), nullable=False)
    )
    state: NotificationState = Field(
        sa_column=Column(
            SAEnum(NotificationState), nullable=False, server_default="queued"
        )
    )
    provider_message_id: str | None = Field(default=None, max_length=255)
    error_message: str | None = Field(default=None, max_length=255)
    sent_at: datetime | None = Field(
        default=None, sa_column=Column(DateTime(timezone=True))
    )

    # ─── Relationships ─────────────────────
    notification: Notification | None = Relationship()
