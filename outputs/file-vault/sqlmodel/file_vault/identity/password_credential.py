# Code generated by @qninhdt/typespec-orm. DO NOT EDIT.
# Source: https://github.com/qninhdt/typespec-libraries

from typing import TYPE_CHECKING
from uuid import UUID
from datetime import datetime

from sqlalchemy import Column, DateTime, ForeignKey
from sqlalchemy.dialects.postgresql import UUID as PG_UUID
from sqlmodel import Field, Relationship, SQLModel
from ..shared.entity import Entity


if TYPE_CHECKING:
    from .user_account import UserAccount


class PasswordCredential(Entity, table=True):
    """Password credential for accounts using local authentication."""

    __tablename__ = "password_credentials"  # type: ignore

    account_id: UUID = Field(
        sa_column=Column(
            PG_UUID(as_uuid=True),
            ForeignKey("user_accounts.id", ondelete="CASCADE"),
            nullable=False,
            index=True,
            unique=True,
        )
    )
    password_hash: str = Field(max_length=255, sa_column_kwargs={"nullable": False})
    password_algorithm: str = Field(max_length=64, sa_column_kwargs={"nullable": False})
    password_updated_at: datetime = Field(
        sa_column=Column(DateTime(timezone=True), nullable=False)
    )
    compromised_at: datetime | None = Field(
        default=None, sa_column=Column(DateTime(timezone=True))
    )

    # ─── Relationships ─────────────────────
    account: UserAccount | None = Relationship()
