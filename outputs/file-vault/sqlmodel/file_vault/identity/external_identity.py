# Code generated by @qninhdt/typespec-orm. DO NOT EDIT.
# Source: https://github.com/qninhdt/typespec-libraries

from typing import TYPE_CHECKING
from uuid import UUID
from pydantic import EmailStr
from datetime import datetime
from enum import Enum

from sqlalchemy import Column, DateTime, Enum as SAEnum, ForeignKey, UniqueConstraint
from sqlmodel import Field, Relationship, SQLModel
from ..shared.entity import Entity


if TYPE_CHECKING:
    from .user_account import UserAccount


class ExternalAuthProvider(str, Enum):
    """Auto-generated enum for external_auth_provider."""

    google = "google"
    github = "github"
    apple = "apple"
    microsoft = "microsoft"


class ExternalIdentity(Entity, table=True):
    """External login identity linked to an account."""

    __tablename__ = "external_identities"  # type: ignore
    __table_args__ = (
        UniqueConstraint(
            "provider",
            "provider_subject",
            name="external_identities_provider_provider_subject_unique",
        ),
    )

    account_id: UUID = Field(
        sa_column=Column(
            ForeignKey("user_accounts.id", ondelete="CASCADE"),
            nullable=False,
            index=True,
        )
    )
    provider: ExternalAuthProvider = Field(
        sa_column=Column(SAEnum(ExternalAuthProvider), nullable=False)
    )
    provider_subject: str = Field(max_length=256, sa_column_kwargs={"nullable": False})
    email_at_provider: EmailStr | None = Field(default=None, max_length=320)
    linked_at: datetime = Field(
        sa_column=Column(DateTime(timezone=True), nullable=False)
    )

    # ─── Relationships ─────────────────────
    account: UserAccount | None = Relationship()
