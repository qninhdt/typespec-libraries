import { z } from "zod";

// Code generated by @qninhdt/typespec-orm. DO NOT EDIT.
export const PublicUserSchema = z
  .object({
    id: z
      .string()
      .describe("Unique identifier shared by most persisted records."),
    email: z
      .string()
      .max(320)
      .email()

        .describe("Email address used for login and invitations. Other namespaces reference this via lookup types."),
    displayName: z
      .string()
      .max(100)
      .describe("Display name shown throughout the UI."),
    avatarUrl: z
      .string()
      .max(512)
      .url()
      .optional()
      .describe("Public avatar URL rendered in lists and profile cards."),
    preferredLocale: z
      .string()
      .max(16)
      .optional()
      .describe("Optional locale preference for formatting and localization."),
    credits: z
      .number()
      .int()
      .default(0)
      .describe("Entitlement credits or consumable balance."),
    isActive: z
      .boolean()
      .default(true)
      .describe("Whether the account is currently active."),
  })

    .describe("Public user record with sensitive and operational fields removed. Demonstrates `OmitProperties` across namespaces.")
export type PublicUser = z.infer<typeof PublicUserSchema>;
