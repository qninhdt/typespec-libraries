# Code generated by @qninhdt/typespec-orm. DO NOT EDIT.
# Source: https://github.com/qninhdt/typespec-libraries

from typing import Any, TYPE_CHECKING

from sqlalchemy import Column, Text
from sqlalchemy.dialects.postgresql import JSONB
from sqlmodel import Field, Relationship, SQLModel
from ...shared.world_owned import WorldOwned


if TYPE_CHECKING:
    from ...worlds.world import World


class GameEvent(WorldOwned, table=True):
    """Event rule that mutates world state when its conditions are met."""

    __tablename__ = "game_events"  # type: ignore

    # Name of the event - indexed for lookup.
    name: str = Field(
        index=True,
        max_length=200,
        sa_column_kwargs={
            "nullable": False,
            "comment": "Name of the event - indexed for lookup.",
        },
    )
    # Narrative description used in AI generation.
    description: str = Field(
        sa_column=Column(
            Text, nullable=False, comment="Narrative description used in AI generation."
        )
    )
    # Predicate expression evaluated at runtime.
    conditions: dict[str, Any] | None = Field(
        default=None,
        sa_column=Column(JSONB, comment="Predicate expression evaluated at runtime."),
    )
    # Mutations applied when the event fires.
    effects: dict[str, Any] | None = Field(
        default=None,
        sa_column=Column(JSONB, comment="Mutations applied when the event fires."),
    )

    # ─── Relationships ─────────────────────
    world: World | None = Relationship()
