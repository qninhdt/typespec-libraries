import { z } from "zod";

// Code generated by @qninhdt/typespec-orm. DO NOT EDIT.
export const PublicUserSchema = z
  .object({
    id: z.string().describe("Unique identifier - use"),
    email: z
      .string()
      .max(320)
      .email()

        .describe("Email address used for login - indexed for fast lookup. Constrained to max 320 characters with @format(\"email\") validation."),
    displayName: z
      .string()
      .max(100)
      .describe("Display name shown in the UI - max 100 characters"),
    avatarUrl: z
      .string()
      .max(512)
      .url()
      .optional()

        .describe("Optional avatar image URL - must be a valid URL, max 512 characters"),
    credits: z
      .number()
      .int()
      .default(0n)
      .describe("Credits balance - defaults to 0 on registration"),
    isActive: z
      .boolean()
      .default(true)

        .describe("Whether the account is active - false for suspended or deactivated accounts"),
  })

    .describe("Public user record with sensitive / internal columns removed. Omits password hash, soft-delete, audit timestamps, and relationships.")
export type PublicUser = z.infer<typeof PublicUserSchema>;
