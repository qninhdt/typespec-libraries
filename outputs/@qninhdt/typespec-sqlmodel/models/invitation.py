# Code generated by @qninhdt/typespec-orm. DO NOT EDIT.
# Source: https://github.com/qninhdt/typespec-libraries

from uuid import UUID, uuid4
from pydantic import EmailStr
from datetime import datetime

from sqlalchemy import Column, DateTime, Text, func
from sqlmodel import Field, SQLModel


class Invitation(SQLModel, table=True):
    """Invitation record that references a user's email and a world's name using lookup types.  The validators (@maxLength, @format, etc.) from the source property should be inherited automatically."""

    __tablename__ = "invitations"  # type: ignore

    # Unique identifier for the invitation
    id: UUID = Field(
        default_factory=uuid4,
        primary_key=True,
        sa_column_kwargs={"comment": "Unique identifier for the invitation"},
    )
    # Email of the invitee - should inherit @maxLength(320) and @format("email") from User.email
    invitee_email: EmailStr = Field(
        max_length=320,
        sa_column_kwargs={
            "nullable": False,
            "comment": 'Email of the invitee - should inherit @maxLength(320) and @format("email") from User.email',
        },
    )
    # Name of the world being shared - should inherit @maxLength(200) from World.name
    world_name: str = Field(
        max_length=200,
        sa_column_kwargs={
            "nullable": False,
            "comment": "Name of the world being shared - should inherit @maxLength(200) from World.name",
        },
    )
    # Optional personal message
    message: str | None = Field(
        default=None,
        max_length=1000,
        sa_column=Column(Text, comment="Optional personal message"),
    )
    # Timestamp when the invitation was sent
    created_at: datetime = Field(
        sa_column=Column(
            DateTime(timezone=True),
            nullable=False,
            server_default=func.now(),
            comment="Timestamp when the invitation was sent",
        )
    )
